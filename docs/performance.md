# DroidLinux Performance Optimization Guide

DroidLinux prioritizes low RAM and fast startup over heavy visual effects.

## Tweak Settings
- Compositor window effects are disabled by default in `config/performance/performance.conf`.
- Mesa GPU virpipe driver is enabled by default where supported.
